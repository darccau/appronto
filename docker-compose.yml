version: '3.8'
services:
  database:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=appronto
      - POSTGRES_PASSWORD=appronto
      - EDITOR=vi
    ports:
      - '54332:5432'
      # cache
